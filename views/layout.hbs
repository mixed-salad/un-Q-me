<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
  <title>UnQ Me {{#if title}}-{{/if}} {{title}}</title>
  <link rel="stylesheet" href="/styles/style.css" />
</head>

<body>
  <nav>
    <a href="/">Home</a>
    {{#if user}}
    <a href="/user/{{ user._id }}">{{user.name}}'s Profile</a>
    <a href="/list/create">Create a shopping list</a>

    <form action="/authentication/log-out" method="POST">
      <button>Log Out</button>
    </form>

    {{else}}
    <a href="/authentication/log-in">Log In</a>
    <a href="/authentication/sign-up">Sign Up</a>
    {{/if}}
  </nav>
  {{{body}}}
  <script src="/scripts/script.js"></script>
</body>

</html>